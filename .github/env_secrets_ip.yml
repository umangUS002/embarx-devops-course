name: Demo CI ENv Secrets

on:
    # push
    workflow_dispatch:

jobs:
    build:
        runs-on: ubuntu-latest

        steps:
            - name: Say Hello
              run: echo "Hello from the 2nd workflow!"

            - name: Install
              env:
                APP_ENV: demo
              run: echo "Environment $APP_ENV"